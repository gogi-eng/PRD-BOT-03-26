# План работ — 30.06.2026 (полный день)

**Ветки дня:** `30.06.26-PRD-BOT-ALL` | `30.06.26-AGENT-WORLD`  
**Контекст:** наблюдение пакета **1+2** (`2eabcdf`) + Hermes bypass (`460cbbf`) с **24.06**.  
**Правила:** push в GitHub после каждого блока; **ZeroOne** — макс. **одна** правка **config** торговли.

---

## Расписание

| Время (UTC+3) | Блок | Что делаем |
|---------------|------|------------|
| **10:00** | A. Торговля | Ветки, статистика, Hermes, решение ZeroOne |
| **12:00–15:00** | B. Инфраструктура | CI, тесты guard, DEPLOY.md, backup — **без смены стратегии** |
| **16:00** | C. Деплой | Только если была **одна** правка config в блоке A |
| **18:00** | D. Фиксация | Дамп PRD, push, проверка напоминалок на сервере |

---

## A. Торговля и наблюдение (10:00)

### A1. Новые ветки (обязательно)

```bash
# PRD
git fetch origin
git checkout 29.06.26-PRD-BOT-ALL 2>/dev/null || git checkout 24.06.26-PRD-BOT-ALL
git pull
git checkout -b 30.06.26-PRD-BOT-ALL
git push -u origin 30.06.26-PRD-BOT-ALL

# WORLD (worktree PRD-BOT-21.06.26-HERMES)
git checkout 29.06.26-AGENT-WORLD 2>/dev/null || git checkout 24.06.26-AGENT-WORLD
git pull
git checkout -b 30.06.26-AGENT-WORLD
git push -u origin 30.06.26-AGENT-WORLD
```

### A2. Статистика с 24.06.2026 (только bot, без manual)

**PRD:**
```bash
cd /root/PRD-BOT-ALL
./venv/bin/python3 scripts/trade_analytics.py --hours 144
# или вручную по journal:
grep '"event":"closed"' data/trades/trade_history.jsonl | ...
```

**WORLD:** то же в `/root/AGENT-WORLD`.

| Метрика | PRD | WORLD |
|---------|-----|-------|
| Закрытий | | |
| Win rate % | | |
| PnL USDT | | |
| Ср. время в позиции | | |
| Закрытий ≈0 после +0.9–1.0% | | |

**Успех пакета 1+2:** меньше «нулей» после импульса, holding **45–90+** мин, SL-hit не взлетел.

### A3. Hermes + логи + inbox

- Pull [Analise_Hermes](https://github.com/gogi-eng/Analise_Hermes) → `meta.json`, `HERMES_LIVE.md`
- `journalctl -u trading_bot --since "2026-06-24"` — нет `early_breakeven`, есть `hermes_bypass L2`
- `journalctl -u trading_bot_agent_world --since "2026-06-24"`
- `wc -l reports/telegram_signals/signals_inbox.jsonl` + топ reject в `telegram_signal_agent`

### A4. Одна правка config (ZeroOne) — выбрать **одну**

| Если… | Действие | Где |
|--------|----------|-----|
| PRD хуже, SL на ретесте | `orderbook_entry` как WORLD | PRD |
| WORLD переторговывает | `max_trades_per_day: 25` | WORLD |
| Риск / просадка PRD | `leverage: 10`, `dynamic_leverage.max: 15` | PRD |
| Мало данных, шум | **Ничего** → наблюдение до **03.07** | — |

**Не делать 30.06:** Speed Mode, снятие max_positions/лимитов, унификация всего трейлинга, block 17–18 без данных.

---

## B. Инфраструктура (12:00–15:00, код — не торговля)

> Из отчёта архитектуры — только **рациональное**. Не трогает входы/выходы в рантайме до merge.

### B1. GitHub Actions CI (приоритет №1)

- [ ] `.github/workflows/pytest.yml` — `pytest backend/tests/` на push в `*-PRD-BOT-ALL` / `*-AGENT-WORLD`
- [ ] Smoke: `test_entry_pipeline`, `test_tp_progress_exit`, `test_supervisor_v4`, `test_hermes_supervisor_bypass`
- [ ] Push в `30.06.26-PRD-BOT-ALL`

### B2. Тесты `prd_agent/risk/guard.py` (приоритет №2)

- [ ] `backend/tests/test_risk_guard_sizing.py` — 5–10 кейсов:
  - daily loss block
  - max_trades_per_day
  - cooldown после убытка
  - расчёт размера (мок баланса)
- [ ] Push; **деплой бота не нужен** (только CI)

### B3. `docs/DEPLOY.md` (приоритет №3)

- [ ] Одна страница: ветка дня → fetch → install_config → restart → grep проверки
- [ ] PRD и WORLD команды рядом
- [ ] Push

### B4. Backup журнала (приоритет №4)

- [ ] `scripts/backup_bot_data.sh` — копия `data/trades/trade_history.jsonl` + `config.yaml` (без `.env`)
- [ ] Cron на сервере: ежедневно 03:30 UTC
- [ ] Push + установка на сервере

### B5. Config validate при старте (если останется время)

- [ ] Проверить: `run_unified.py` падает с понятной ошибкой при битом yaml
- [ ] Иначе — задача на **03.07**

---

## C. Деплой торговли (16:00 — только если A4 менял config)

```bash
cd /root/PRD-BOT-ALL
git fetch origin 30.06.26-PRD-BOT-ALL
git reset --hard origin/30.06.26-PRD-BOT-ALL
bash scripts/install_production_config.sh
sudo systemctl restart trading_bot

cd /root/AGENT-WORLD
git fetch origin 30.06.26-AGENT-WORLD
git reset --hard origin/30.06.26-AGENT-WORLD
bash scripts/install_agent_world_config.sh
sudo systemctl restart trading_bot_agent_world
```

Проверка PRD:
```bash
grep -E "early_breakeven_enabled|breakeven_at_profit_pct|sl_sr_level_index" /root/PRD-BOT-ALL/config.yaml
```

---

## D. Фиксация (18:00)

- [ ] Свежий дамп: `python scripts/dump_unified_bot.py` → `BOT_DUMP_PRD-BOT-ALL_30.06.26.txt` → push
- [ ] Сервер: `bash scripts/register_checkpoint_30_06_reminder.sh` (если ещё не стояло)
- [ ] Записать итог дня в `.cursor/REMINDER_30_06_26.md` (таблица PRD vs WORLD + что решили)

---

## Явно НЕ на 30.06 (перенос)

| Задача | Когда |
|--------|-------|
| Микросервисы, Redis, PostgreSQL | не планируется |
| Удаление `legacy/` | после CI + grep зависимостей (**июль**) |
| Полный split `orchestrator.py` | поэтапно (**июль–август**) |
| mypy --strict, DI везде | низкий приоритет |
| Prometheus / ELK | не нужно на 1 VPS |
| Шифрование .env KMS | не нужно |
| Массовая замена urllib→aiohttp | по мере правок файлов |

---

## Следующие даты

| Дата | Что |
|------|-----|
| **03.07.2026** | Финал наблюдения / вторая ZeroOne / leverage PRD |
| **Июль** | Вынести SignalCollector из orchestrator (1 модуль) |
| **01.08.2026** | Решение по удалению legacy (если CI зелёный) |

---

## Cursor

Написать: **«план 30.06»** — агент читает этот файл и ведёт по блокам A→D.
