# План работ — 30.06.2026

**Контекст:** наблюдение после пакета **1+2** (PRD `2eabcdf`) и **Hermes bypass L2** (`460cbbf`).  
Старт наблюдения: **24.06.2026**. Чекпоинт: **30.06.2026** (6 суток).  
**Правило:** новая ветка каждый день; **ZeroOne** — максимум **одна** правка config за цикл.

---

## Утро (ветки + сверка)

### 1. Новые ветки дня (обязательно)

```bash
# Локально / GitHub — от вчерашней 29.06.26-* или от 24.06.26-* если 29 не было
git checkout 24.06.26-PRD-BOT-ALL   # или последняя дневная
git pull
git checkout -b 30.06.26-PRD-BOT-ALL
git push -u origin 30.06.26-PRD-BOT-ALL

# WORLD — в worktree PRD-BOT-21.06.26-HERMES или отдельный clone
git checkout 24.06.26-AGENT-WORLD
git pull
git checkout -b 30.06.26-AGENT-WORLD
git push -u origin 30.06.26-AGENT-WORLD
```

### 2. Статистика с 24.06.2026 UTC (оба бота)

Собрать по **только bot** (без manual) с `trade_history.jsonl`:

| Метрика | PRD | WORLD |
|---------|-----|-------|
| Закрытий | | |
| Win rate % | | |
| PnL USDT | | |
| Среднее время в позиции | | |
| Закрытий «≈0» после +0.9–1.0% | | |

**Критерии успеха пакета 1+2 (PRD):**
- меньше микро-закрытий в ноль после импульса;
- дольше holding time (цель 45–90+ мин vs 15–30);
- SL-hit не вырос катастрофически.

### 3. Hermes + inbox

- `git pull` в [Analise_Hermes](https://github.com/gogi-eng/Analise_Hermes) → `HERMES_LIVE.md`, `meta.json` fingerprint.
- PRD: `journalctl -u trading_bot --since "2026-06-24"` — нет ли `early_breakeven`, есть ли `hermes_bypass L2`.
- WORLD: `journalctl -u trading_bot_agent_world --since "2026-06-24"`.
- Проверить `signals_inbox.jsonl`: пустой или есть записи; топ reject в логах `telegram_signal_agent`.

---

## День (решения — только по цифрам)

### Матрица «одна правка ZeroOne» (выбрать **не больше одной**)

| Условие 30.06 | Действие | Где |
|----------------|----------|-----|
| PRD WR/PnL хуже WORLD, много SL на ретесте | Включить `orderbook_entry` (как WORLD) | **только PRD** |
| WORLD переторговывает (>25 сделок/сутки или PnL−) | `max_trades_per_day: 25` | **только WORLD** |
| Общий риск / просадка на PRD | `dynamic_leverage.max: 15`, `leverage: 10` | **только PRD** |
| Статистика rule weights шумная | `rule_weight_learning.min_samples: 20` | оба (если одна правка — приоритет ниже) |
| Всё в пределах шума, мало сделок | **Ничего не менять** — продлить наблюдение до **03.07** | — |

**Не делать 30.06 без отдельного решения:**
- Speed Mode / ослабление Quality Gate 0.92;
- снять max_positions / дневной лимит убытка;
- унифицировать весь трейлинг PRD=WORLD;
- убрать block 17–18 без статистики Hermes.

---

## Вечер (деплой, если была одна правка)

```bash
# PRD
cd /root/PRD-BOT-ALL
git fetch origin 30.06.26-PRD-BOT-ALL
git reset --hard origin/30.06.26-PRD-BOT-ALL
bash scripts/install_production_config.sh
sudo systemctl restart trading_bot

# WORLD
cd /root/AGENT-WORLD
git fetch origin 30.06.26-AGENT-WORLD
git reset --hard origin/30.06.26-AGENT-WORLD
bash scripts/install_agent_world_config.sh
sudo systemctl restart trading_bot_agent_world
```

Проверка config (PRD):

```bash
grep -E "early_breakeven_enabled|breakeven_at_profit_pct|sl_sr_level_index" /root/PRD-BOT-ALL/config.yaml
```

Ожидается: `early_breakeven_enabled: false`, `breakeven_at_profit_pct: 1.05`, `sl_sr_level_index: 1`.

---

## Опционально (если останется время, без config)

- [ ] Свежий дамп PRD: `python scripts/dump_unified_bot.py ...` → commit + push
- [ ] Обновить `CONFIG_GUIDE.md` — убрать устаревшие параметры (из отчёта 24.06)
- [ ] Cron backup: `trade_history.jsonl` + `config.yaml` (без `.env`) на GitHub или rsync

---

## Напоминание агенту Cursor

В сессии **30.06.2026** написать: **«план 30.06»** или **«чекпоинт наблюдения»** — агент читает этот файл и `Analise_Hermes/meta.json`.

---

## Следующий чекпоинт

**03.07.2026** — финал наблюдения или вторая ZeroOne-правка по итогам 30.06.
