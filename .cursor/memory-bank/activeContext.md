# Active Context

**ИП ОТ/ПБ (02.09.2026):** чат сохранён в `.cursor/chats/archive/02.09.26-ИП-ОТ-ПБ.md`, ветка `02.09.26-ИП-ОТ-ПБ`. Прайс ot-pb.by — регистрировать **ИП** (не НПД). Основной ОКРБ **74909**. Затем аккредитация Минтруда. Лицензируемый промбез — только юрлицо.

**Проверка знаний Госпромнадзор (31.08.2026):** агент «Проверка_знаний-Госпромнадзор». Почта Roundcube (не Outlook). После отбора — Excel с файлами для подразделений на проверку, письма только по кнопке «Отправить письма». После докладной — Word на проверку, в 1С пользователь отправляет сам. Ярлык 1С агент не запускает.

**Делопроизводитель / пути (28.08.2026):** исходник и образец — любой указанный файл (Загрузки, ОБМЕН, САТП, Агент, Проекты). Запись — поле «Результат»; предупреждение при ОБМЕН/САТП, без блокировки. Не сканировать весь N:\. Сборка `2026-08-28-any-path`. Skill `sniot-user-folder-only` / `sniot-damage-acts`.

**Делопроизводитель / перенос в форму (28.08.2026):** исходник, форма и результат задаются явно (поля окна / handoff), не по имени файла. Подписанты заполненного акта: интервал **1,5**, ФИО на **12 см**. Skill `sniot-damage-acts`. Не выдумывать поля вне исходника; бэкапов нет, пока не попросят.

**Офис-агенты в git (28.08.2026):** снимок с ПК запушен в `gogi-eng/PRD-BOT-03-26`:
- `28.08.26-DocAgent` / `DocAgent` @ `f7b7619` — делопроизводитель + AttestationSync
- `28.08.26-Еженедельный_итог` / `Еженедельный_итог` @ `ad2940d`

---

**Дата фокуса:** 28.08.2026 (UTC+3)

**Техническая учёба (28.08):** доклады, презентации и фильм в `Desktop\Техническая_учеба_2026\` (копия в `Desktop\Нормативка\Техническая_учеба_2026\`):
1. Оформление документации по проверке знаний по промбезу (ПМЧС № 31 с изм. № 55 от 17.07.2024).
2. Работы стреловыми кранами (ППОПБ, ПМЧС № 66 с изм. № 22 от 25.03.2024).
3. Фильм: комиссия, программа «Экзамен», заполнение протокола стропальщика и машиниста автокрана.

---

**Дата фокуса (бот):** 14.08.2026 (UTC+3)
**Ветки дня:** `09.08.26-PRD-BOT-ALL` @ `000ba22` · `09.08.26-AGENT-WORLD` @ `c430adc`

## Текущий фокус

0. **СНиОТ делопроизводитель (28.08, сборка `2026-08-28-any-path`):**
   - Исходник и образец — любой указанный путь.
   - Запись — поле «Результат»; пусто — рядом с исходником.
   - Закрыть DocAgent → `C:\Users\v.dubovik\DocAgent\start_agent.bat` → в «Готово»: `СНиОТ: сборка 2026-08-28-any-path`.

1. **Hotfix 08.08 → оба инстанса (09.08):**
   - Manual/Companion: `manual_auto_close: false`, `auto_close_manual: false` (код уже был в ветках дня)
   - SPIKE P0: `zone_corridor_play.spike_bypass_no_corridor: true` — **прод + песочница**
   - SPIKE P1: `spike_scalp.pullback_entry.enabled: true` — **прод + песочница** (`min_retrace_pct: 0.15`)
   - Прод SPIKE loop: `run_loop_in_signal_agent: true` (не трогать)
2. **SL/TP guard (09.08):** `positions.sl_tp_guard` — восстановление пустых SL/TP на бирже; `include_manual: true`
3. Wallet / SPIKE / polling / фильтры / bybit_monitor — **не отключать**

## Маркеры логов

| Что | Маркер |
|-----|--------|
| Manual time-stop skip | `MANUAL SAFE skip time-stop` |
| Companion skip manual | `Companion skip manage` / `Companion skip close` |
| Zone corridor | `Zone corridor` |
| SPIKE bypass | `SPIKE bypass no_corridor` |
| SPIKE pullback | `SPIKE pullback:` |
| SL/TP guard | `Missing SL/TP on position`, `SL/TP guard` |

## Сервер

| Параметр | Значение |
|----------|----------|
| IP | 207.154.238.178 |
| User | root |
| Прод | /root/PRD-BOT-ALL |
| Песочница | /root/AGENT-WORLD |

## Не смешивать

- /root/PRD-BOT-ALL ← только *-PRD-BOT-ALL
- /root/AGENT-WORLD ← только *-AGENT-WORLD
