# Задания для Cursor / DocAgent

## ГЛАВНОЕ — правка только в «Агент»; ОБМЕН — образец read-only

**Правка (запись) — только папка:**

```
N:\9 - Служба надёжности и охраны труда (СНiОТ)\Дубовик В.В\Агент\
```

Файлы пользователя:

- `ПРОЕКТ Старший мастер_оформлен.docx`
- `ПРОЕКТ Старший мастер_оформлен+.docx`

**Образцы из `!!!ОБМЕН\РАССМОТРЕНИЕ  ДИ, РИ, ПОЛОЖЕНИЙ\`** — можно **читать** конкретный эталон для сравнения.  
**Нельзя:** править, сохранять, открывать соседние файлы, сканировать диск.

Правило Cursor: `.cursor/rules/sniot-user-folder-only.mdc`

## Как указать путь

1. Поле **«1. Документ»** — файл в папке **Агент**
2. `handoff\request_latest.json` → `"source_path"` — только если файл в Агент

В чат Cursor путь вставлять не нужно.

## Запуск

- Щёлкните **аватар** → укажите файл в Агент → **«Оформить документ»**
- Скрипт: `C:\Users\v.dubovik\AttestationSync\fix_sniot_document.py`
