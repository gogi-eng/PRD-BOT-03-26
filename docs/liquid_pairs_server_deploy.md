# ????????? ????? ????????? ??? ?? ???????

????????? ????? **?? ??????** ???????? ????? (`PRD-BOT-ALL` ? `AGENT-WORLD`).

## ???? ????????

| ??? | ???? |
|-----|------|
| ??? | `/root/LIQUID-PAIRS-REPORT` |
| ??? ???????? | `/root/LIQUID-PAIRS-REPORT/data/reports/hourly_run.log` |
| ????????? ????? | `/root/LIQUID-PAIRS-REPORT/data/reports/liquid_pairs_latest.md` |
| ??????? Telegram | `/root/LIQUID-PAIRS-REPORT/.env` (?? ? git) |
| Cron | ?????? ??? ? `:00` (UTC ??????? = ??? ????? ??? ? ???) |

????? ??? ???? ?????: `28.07.26-LIQUID-PAIRS`.

?????? ?????? ?????? ????????? API Bybit ? ???? ????? ? Telegram. ?????? ?? ??????. `trading_bot` / `telegram_signal_agent` **?? ?????????????**.

## ?????? ?????? (SSH ?? DigitalOcean)

```bash
# 1) ???? (shallow) ? ????????? ?????
mkdir -p /root
cd /root
git clone --depth 1 -b 28.07.26-LIQUID-PAIRS \
  https://github.com/gogi-eng/PRD-BOT-03-26.git LIQUID-PAIRS-REPORT

# 2) ??????? Telegram (???? .env ? ????? ??? ?? ???, ??? ? AGENT-WORLD)
cd /root/LIQUID-PAIRS-REPORT
cp .env.example .env 2>/dev/null || true
nano .env
```

? `.env` ??? ?????? (??? ???????, ??? ???????? ?????? `=`):

```
TELEGRAM_TOKEN=????_?????_????
TELEGRAM_CHAT_ID=????_id_????_???_??????
```

????? ????? ???????? ?? ????????? (?? ????????? ???? ????, ?????? ??? ??? ??????):

```bash
grep -E '^TELEGRAM_(TOKEN|CHAT_ID)=' /root/AGENT-WORLD/.env
```

???? ???????? **??????????** ???? ? @BotFather ? ????? ?????? ?? ????????? ? ???????? ????????.

```bash
# 3) ????? + cron (??? restart ???????? ????????)
chmod +x scripts/*.sh scripts/hourly_liquid_pairs_report.py
bash scripts/deploy_liquid_pairs_report.sh /root/LIQUID-PAIRS-REPORT 28.07.26-LIQUID-PAIRS
```

???? ????? ??? ????????????, `deploy_liquid_pairs_report.sh` ??? ??????? `git fetch` + `reset --hard FETCH_HEAD` ? ???????? cron.

## ????????

```bash
# ?????? ?????? (?????? ????????? ????????? ? Telegram: ?????? ??? ??????? ????)
cd /root/LIQUID-PAIRS-REPORT
bash scripts/run_hourly_liquid_pairs.sh

# ??? ? ?????
tail -40 data/reports/hourly_run.log
ls -la data/reports/liquid_pairs_latest.md

# Cron ?? ?????
crontab -l | grep -A3 LIQUID-PAIRS
```

? ???? ????? ?????? `telegram ok` ??? `telegram skip: no credentials` (????? ????????? `.env`).

## ?????????? ???? ?????

```bash
cd /root/LIQUID-PAIRS-REPORT
bash scripts/deploy_liquid_pairs_report.sh /root/LIQUID-PAIRS-REPORT 28.07.26-LIQUID-PAIRS
git rev-parse --short HEAD
```

`.env` ?????? ????????? ????????? (????? ?? `/tmp`).

## ?????? ?????? cron (????? ????? ????????)

```bash
bash /root/LIQUID-PAIRS-REPORT/scripts/install_liquid_pairs_cron.sh --remove
```

## ??? ??? ?? ??????

- ?? ?????? `/root/PRD-BOT-ALL` ? `/root/AGENT-WORLD`
- ?? ???????? `systemctl restart trading_bot*`
- ?? ?????? API-????? Bybit (????????? ?????)
- ?? ???????
